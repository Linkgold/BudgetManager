namespace Domain.Enums
{
    public enum BudgetStatus
    {
        Green = 1,      // Less than 80% spent
        Yellow = 2,     // 80-100% spent
        Red = 3         // More than 100% spent
    }
}
